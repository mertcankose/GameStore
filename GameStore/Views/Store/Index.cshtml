<!DOCTYPE html>
<html>
<head>
    <title>Game Store - Product Detail</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" />
    <link rel="stylesheet" href="~/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="#"><i class="fas fa-gamepad"></i> Game Store</a>
        </div>
    </nav>

    <div class="container">
        <h3 class="game-store-title text-center">Product Detail</h3>
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card product-detail-card">
                    <img src="@Model.Image" class="card-img-top" alt="Product Image">
                    <div class="card-body">
                        <h5 class="card-title">@Model.Name</h5>
                        <p class="card-text">@Model.Description</p>
                        <p class="card-text price-text">Price: $@Model.Price</p>
                        <form method="post" action="AddProductToCart">
                            <input type="hidden" name="Name" value="@Model.Name" />
                            <input type="hidden" name="Description" value="@Model.Description" />
                            <input type="hidden" name="Image" value="@Model.Image" />
                            <input type="hidden" name="Price" value="@Model.Price" />
                            <button type="submit" class="btn add-to-cart-btn">Add to Cart</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="footer">
        <p>©Game Store 2023</p>
    </footer>
</body>
</html>
